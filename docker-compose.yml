version: '3.4'

services:
  nginx:
    image: nginx
    volumes:
      - ./conf/nginx.conf:/etc/nginx/nginx.conf
      - ./conf/proxy.conf:/etc/nginx/proxy.conf
      - ./logs:/usr/nginx/logs
    network_mode: 'host'